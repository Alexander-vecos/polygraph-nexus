# Pages — quick guide

...existing code...

- Template & example:
  - Use web/src/pages/PageTemplate.tsx to scaffold pages quickly.
  - See web/src/pages/NewPageExample.tsx for a copyable example.
  - To expose the page, add a route in web/src/router.tsx (import and add a route).

...existing code...# Pages — quick guide

...existing code...

- Template & example:
  - Use web/src/pages/PageTemplate.tsx to scaffold pages quickly.
  - See web/src/pages/NewPageExample.tsx for a copyable example.
  - To expose the page, add a route in web/src/router.tsx (import the page and add a route, e.g. { path: '/app/new', element: <NewPageExample /> }).

...existing code...